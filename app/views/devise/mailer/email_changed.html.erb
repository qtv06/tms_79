<p><%= t ".hello", email: @email %></p>

<% if @resource.try(:unconfirmed_email?) %>
  <p><%= t ".contact_change", email: @resource.unconfirmed_email %></p>
<% else %>
  <p><%= t ".contact_has_change", email: @resource.email %></p>
<% end %>
