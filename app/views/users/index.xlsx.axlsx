wb = xlsx_package.workbook
wb.styles do |style|
  highlight_cell = style.add_style(bg_color: "EFC376")

  wb.add_worksheet(name: "User") do |sheet|
    sheet.add_row %w(name email)
    @users.each do |user|
      sheet.add_row [user.name, user.email], style: [nil, highlight_cell]
    end
  end
end
